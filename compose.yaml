services:
  db:
    image: postgres:16-alpine
    environment:
      POSTGRES_USER: qfl
      POSTGRES_PASSWORD: qfl
      POSTGRES_DB: qfl
    ports: ["5433:5432"]
    volumes: [qfl-pgdata:/var/lib/postgresql/data]
volumes:
  qfl-pgdata:
